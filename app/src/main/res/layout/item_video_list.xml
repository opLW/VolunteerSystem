<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_marginStart="@dimen/activity_h_min_margin"
    android:layout_marginEnd="@dimen/activity_h_min_margin"
    android:layout_marginTop="@dimen/activity_v_min_margin"
    android:layout_marginBottom="@dimen/activity_v_min_margin"
    android:padding="10dp"
    android:background="@drawable/bg_common_item">

    <TextView
        android:id="@+id/video_item_name_tv"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toEndOf="@id/video_item_iv"
        android:layout_marginStart="@dimen/activity_h_min_margin"
        android:layout_centerVertical="true"
        android:singleLine="true"
        android:textSize="@dimen/text_mid_size"
        android:textColor="@color/colorText"
        tools:text="视频一"/>

    <ImageView
        android:id="@+id/video_item_iv"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:layout_alignParentStart="true"
        android:layout_centerVertical="true"
        android:background="@drawable/ic_play"
        android:elevation="10dp"/>
</RelativeLayout>